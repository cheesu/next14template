"use client";
import React, { useEffect, useRef } from 'react';
import ExternalDicomViewer from './DicomViewer'

const DicomViewer = () => {
  
  return (
   <div>
    <ExternalDicomViewer />
   </div>
  );
}

export default DicomViewer;
